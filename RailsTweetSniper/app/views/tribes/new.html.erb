<h1>New Tribe</h1>

<%= form_for(@tribe, :url => {:controller => "tribes", :action => "create", :user_id => @current_user_id}) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f, :command => "new"} %>
<% end %>

<%= link_to 'Back', user_tribes_path(@current_user_id) %>

